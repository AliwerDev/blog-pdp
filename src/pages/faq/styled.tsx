import styled from "@emotion/styled";

const Styled = styled.div`
  padding-inline: 30px;

  .container {
    padding-top: 30px;
    max-width: 1200px;
    margin: 0 auto;
  }
`;

export default Styled;
